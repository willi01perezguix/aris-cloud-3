# PyInstaller spec template for ARIS CORE 3
block_cipher = None

a = Analysis(['../aris_core_3_app/src/aris_core_3_app/app.py'], pathex=['..'], binaries=[], datas=[], hiddenimports=['aris3_client_sdk','tkinter'], hookspath=[], runtime_hooks=[], excludes=[], win_no_prefer_redirects=False, win_private_assemblies=False, cipher=block_cipher)
pyz = PYZ(a.pure, a.zipped_data, cipher=block_cipher)
exe = EXE(pyz, a.scripts, a.binaries, a.zipfiles, a.datas, name='ARIS-CORE-3', console=False)
